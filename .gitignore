terraton_fan_app/build/
terraton_fan_app/.dart_tool/
terraton_fan_app/.flutter-plugins
terraton_fan_app/.flutter-plugins-dependencies
builds/*.apk
builds/version.json

# Build tooling caches / generated output
.wrangler/
cloudflare/.wrangler/
graphify-out/
build_output.log

# Local secrets — never commit
secrets.env
ml/.r2.env
*.keystore

# Terraton's proprietary MCU firmware source — reference only, never publish.
# This repo is public. FW_bug.md quotes short snippets for the vendor bug
# report; the full files stay local.
IRScan.c
Customer.h
